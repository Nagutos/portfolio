# Contact form endpoint (third-party service such as Formspree).
# Create a form at https://formspree.io, then copy your endpoint here into a
# local .env.local file (which is gitignored and never committed).
NEXT_PUBLIC_CONTACT_ENDPOINT=https://formspree.io/f/your_form_id
